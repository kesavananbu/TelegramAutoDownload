<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- Copy all NuGet assemblies to the build output so Ytdlp.NET.dll is available for dynamic plugin loading -->
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Ytdlp.NET" Version="3.0.4" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\BasePlugin\BasePlugins.csproj" />
  </ItemGroup>

  <!-- After each build, deploy the plugin DLLs + all NuGet dependencies to the main app's Plugins\SocialMedia folder -->
  <Target Name="DeployPlugin" AfterTargets="Build">
    <PropertyGroup>
      <PluginsDir>$(MSBuildProjectDirectory)\..\TelegramAutoDownload\bin\$(Configuration)\net8.0-windows\Plugins\SocialMedia</PluginsDir>
    </PropertyGroup>
    <ItemGroup>
      <SocialMediaOutputDlls Include="$(OutputPath)*.dll" Exclude="$(OutputPath)BasePlugins.dll" />
    </ItemGroup>
    <MakeDir Directories="$(PluginsDir)" />
    <Copy SourceFiles="@(SocialMediaOutputDlls)" DestinationFolder="$(PluginsDir)" SkipUnchangedFiles="true" />
  </Target>
</Project>
