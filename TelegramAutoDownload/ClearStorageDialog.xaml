<mah:MetroWindow x:Class="TelegramAutoDownload.ClearStorageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Clear Storage"
        Height="620" Width="780"
        MinHeight="420" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Date range filter + options -->
        <Border Grid.Row="0" Background="#F5F5F5" CornerRadius="6" Padding="16,12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Filter by last-modified date" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="From:" VerticalAlignment="Center"
                               FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpFrom" Grid.Column="1" Height="32"/>

                    <TextBlock Grid.Column="3" Text="To:" VerticalAlignment="Center"
                               FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpTo" Grid.Column="4" Height="32"/>

                    <Button Grid.Column="6" x:Name="btnScan" Content="Scan"
                            Height="32" Width="80" HorizontalAlignment="Right"
                            Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                            Click="BtnScan_Click"/>
                </Grid>

                <!-- Filter options row -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <CheckBox x:Name="chkIncompleteOnly"
                              Content="Show incomplete (.part) files only"
                              IsChecked="False"
                              VerticalAlignment="Center"
                              Checked="FilterChanged" Unchecked="FilterChanged"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Summary bar -->
        <Border Grid.Row="1" Background="#E8F5E9" CornerRadius="6" Padding="12,8"
                BorderBrush="#A5D6A7" BorderThickness="1" Margin="0,0,0,8"
                x:Name="pnlSummary" Visibility="Collapsed">
            <TextBlock x:Name="tbSummary" Foreground="#2E7D32" FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Text="0 files found — Total size: 0 B"/>
        </Border>

        <!-- Scanning indicator -->
        <Border Grid.Row="2" Margin="0,0,0,4" x:Name="pnlScanning" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <mah:ProgressRing Width="20" Height="20" IsActive="True" Margin="0,0,8,0"/>
                <TextBlock Text="Scanning…" VerticalAlignment="Center" Foreground="#888"/>
            </StackPanel>
        </Border>

        <!-- File list -->
        <Border Grid.Row="3" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
            <ListView x:Name="listFiles" BorderThickness="0"
                      SelectionMode="Extended">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="File Name" Width="250"
                                        DisplayMemberBinding="{Binding FileName}"/>
                        <GridViewColumn Header="Size" Width="80"
                                        DisplayMemberBinding="{Binding SizeFmt}"/>
                        <GridViewColumn Header="Type" Width="70"
                                        DisplayMemberBinding="{Binding FileType}"/>
                        <GridViewColumn Header="Last Modified" Width="140"
                                        DisplayMemberBinding="{Binding LastModifiedFmt}"/>
                        <GridViewColumn Header="Full Path" Width="240"
                                        DisplayMemberBinding="{Binding FullPath}"/>
                    </GridView>
                </ListView.View>
                <ListView.Style>
                    <Style TargetType="ListView">
                        <Style.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListView">
                                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                <TextBlock Text="No files found. Press Scan to search."
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Foreground="#AAAAAA" FontSize="12" Margin="0,40"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Style>
                <!-- Highlight .part files in light red -->
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsIncomplete}" Value="True">
                                <Setter Property="Background" Value="#FFEBEE"/>
                                <Setter Property="Foreground" Value="#B71C1C"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Border>

        <!-- Action buttons -->
        <Grid Grid.Row="5" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="btnSelectAll" Content="Select All" Height="32" Width="90"
                        Margin="0,0,8,0" Click="BtnSelectAll_Click"/>
                <Button x:Name="btnDeselectAll" Content="Deselect All" Height="32" Width="90"
                        Click="BtnDeselectAll_Click"/>
            </StackPanel>

            <Button Grid.Column="1" x:Name="btnDelete"
                    Height="32" Width="150"
                    IsEnabled="False"
                    Foreground="White"
                    Background="#D32F2F"
                    BorderBrush="#B71C1C"
                    Content="Delete Selected (0)"
                    Click="BtnDelete_Click"/>

            <Button Grid.Column="3" Content="Close" Height="32" Width="80"
                    Click="BtnClose_Click"/>
        </Grid>
    </Grid>

</mah:MetroWindow>
