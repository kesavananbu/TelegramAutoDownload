<mah:MetroWindow x:Class="TelegramAutoDownload.ClearStorageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Clear Storage"
        Height="580" Width="700"
        MinHeight="400" MinWidth="550"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header description -->
        <Border Grid.Row="0" Background="#FFF3E0" CornerRadius="6" Padding="12,10"
                BorderBrush="#FFCC80" BorderThickness="1" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="ℹ" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="#E65100"/>
                <TextBlock TextWrapping="Wrap" Foreground="#5D4037" FontSize="11">
                    <Run FontWeight="SemiBold">Incomplete downloads</Run>
                    <Run>are stored as </Run>
                    <Run FontWeight="SemiBold">.part</Run>
                    <Run> files. Deleting them frees disk space. Recent .part files may be resumable — check the date before deleting.</Run>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Date range filter + Scan button -->
        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="6" Padding="16,12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Filter by last-modified date" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="From:" VerticalAlignment="Center"
                               FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpFrom" Grid.Column="1" Height="32"/>

                    <TextBlock Grid.Column="3" Text="To:" VerticalAlignment="Center"
                               FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <DatePicker x:Name="dpTo" Grid.Column="4" Height="32"/>

                    <Button Grid.Column="6" x:Name="btnScan" Content="Scan"
                            Height="32" Width="80" HorizontalAlignment="Right"
                            Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                            Click="BtnScan_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Summary bar -->
        <Border Grid.Row="2" Background="#E8F5E9" CornerRadius="6" Padding="12,8"
                BorderBrush="#A5D6A7" BorderThickness="1" Margin="0,0,0,8"
                x:Name="pnlSummary" Visibility="Collapsed">
            <DockPanel>
                <TextBlock x:Name="tbSummary" Foreground="#2E7D32" FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Text="0 files found — Space to free: 0 B"/>
            </DockPanel>
        </Border>

        <!-- File list -->
        <Border Grid.Row="3" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
            <ListView x:Name="listFiles" BorderThickness="0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="File Name" Width="220"
                                        DisplayMemberBinding="{Binding FileName}"/>
                        <GridViewColumn Header="Size" Width="80"
                                        DisplayMemberBinding="{Binding SizeFmt}"/>
                        <GridViewColumn Header="Last Modified" Width="140"
                                        DisplayMemberBinding="{Binding LastModifiedFmt}"/>
                        <GridViewColumn Header="Full Path" Width="220"
                                        DisplayMemberBinding="{Binding FullPath}"/>
                    </GridView>
                </ListView.View>
                <ListView.Style>
                    <Style TargetType="ListView">
                        <Style.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListView">
                                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                <TextBlock Text="No .part files found. Press Scan to search."
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Foreground="#AAAAAA" FontSize="12" Margin="0,40"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.Style>
            </ListView>
        </Border>

        <!-- Scanning indicator (shown while scan is running) -->
        <Border Grid.Row="4" Margin="0,8,0,0" x:Name="pnlScanning" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <mah:ProgressRing Width="20" Height="20" IsActive="True" Margin="0,0,8,0"/>
                <TextBlock Text="Scanning…" VerticalAlignment="Center" Foreground="#888"/>
            </StackPanel>
        </Border>

        <!-- Action buttons -->
        <Grid Grid.Row="5" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="tbDeleteHint"
                       Foreground="#888" FontSize="11" VerticalAlignment="Center"
                       Text="Scan first to see which files will be deleted."/>

            <Button Grid.Column="1" x:Name="btnDelete"
                    Height="32" Width="130"
                    IsEnabled="False"
                    Foreground="White"
                    Background="#D32F2F"
                    BorderBrush="#B71C1C"
                    Content="Delete 0 files"
                    Click="BtnDelete_Click"/>

            <Button Grid.Column="3" Content="Close" Height="32" Width="80"
                    Click="BtnClose_Click"/>
        </Grid>
    </Grid>

</mah:MetroWindow>
